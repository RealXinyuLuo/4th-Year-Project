% Using a global variable to pass target image into the neural network
% This is legacy code from Justin's project
% No longer used in Xinyu's code

function r = getGlobalImage
  global image
  r = image;
end
